{
  "name": "ai-capture-analyser",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc",
    "deploy": "wrangler deploy",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "*",
    "typescript": "*",
    "wrangler": "*"
  }
}